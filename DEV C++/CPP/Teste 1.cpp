#include<stdio.h>
#include<cstdio>
#include<iostream.h>
#include<stdlib.h>
main()
{
      int x,y,r;
      printf("Digite dois valores:");
      scanf(" %i %i",&x, &y);
            r=x+y;
      printf("A soma dos dois valores foi:%i",r);
      system("pause");
      }
